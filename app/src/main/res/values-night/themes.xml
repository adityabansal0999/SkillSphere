<resources>
    <!-- Base Application Theme -->
    <style name="Theme.SkillSphere" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary Colors -->
        <item name="colorPrimary">@color/green</item>
        <item name="colorPrimaryVariant">@color/green_dark</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary Colors -->
        <item name="colorSecondary">@color/green_mid</item>
        <item name="colorSecondaryVariant">@color/green_dark</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Background Colors -->
        <item name="android:colorBackground">@color/background</item>
        <item name="colorSurface">@color/white</item>

        <!-- Status Bar -->
        <item name="android:statusBarColor">@color/green_mid</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Navigation Bar -->
        <item name="android:navigationBarColor">@color/white</item>
        <item name="android:windowLightNavigationBar">true</item>

        <!-- Typography -->
        <item name="fontFamily">@font/inter</item>
        <item name="android:fontFamily">@font/inter</item>

        <!-- Material Components -->
        <item name="materialButtonStyle">@style/Widget.SkillSphere.Button</item>
        <item name="textInputStyle">@style/Widget.SkillSphere.TextInputLayout</item>
        <item name="bottomNavigationStyle">@style/Widget.SkillSphere.BottomNavigationView</item>
    </style>

    <!-- Splash Theme -->
    <style name="Theme.SkillSphere.Splash" parent="Theme.SkillSphere">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:statusBarColor">@color/green_mid</item>
    </style>

    <!-- Button Style -->
    <style name="Widget.SkillSphere.Button" parent="Widget.MaterialComponents.Button">
        <item name="cornerRadius">12dp</item>
        <item name="android:textSize">15sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:paddingTop">14dp</item>
        <item name="android:paddingBottom">14dp</item>
    </style>

    <!-- TextInputLayout Style -->
    <style name="Widget.SkillSphere.TextInputLayout" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="boxStrokeColor">@color/green</item>
        <item name="hintTextColor">@color/green</item>
    </style>

    <!-- Bottom Navigation Style -->
    <style name="Widget.SkillSphere.BottomNavigationView" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="itemIconTint">@color/bottom_nav_selector</item>
        <item name="itemTextColor">@color/bottom_nav_selector</item>
        <item name="labelVisibilityMode">labeled</item>
    </style>

    <!-- Card Style -->
    <style name="Widget.SkillSphere.CardView" parent="Widget.MaterialComponents.CardView">
        <item name="cardCornerRadius">12dp</item>
        <item name="cardElevation">1dp</item>
        <item name="cardBackgroundColor">@color/white</item>
    </style>
</resources>